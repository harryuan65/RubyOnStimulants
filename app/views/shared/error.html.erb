<%= stylesheet_link_tag('error') %>
<%= link_to(t('controller.general.go_home'), root_path) %>
<div class="container notosans text-dk error">
  <h1 class="error-title"><%= @i18n_message %></h1>
  <% if current_user && current_user.admin? %>
    <h2 class="error-subtitle"><%= @error_message %></h2>
  <% end %>
</div>